// =====================
// Stylelint Configuration for Next.js + Tailwind CSS
// =====================
// This file configures Stylelint to enforce consistent and modern CSS code style.
// - stylelint-config-standard: Standard CSS style guide
// - stylelint-config-tailwindcss: Tailwind CSS-specific linting rules

module.exports = {
  extends: [
    "stylelint-config-standard",      // Standard CSS linting rules
    "stylelint-config-tailwindcss"    // Tailwind CSS-specific linting rules
  ],
};
